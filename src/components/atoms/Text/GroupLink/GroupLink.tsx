import React from 'react';
import { Container, Text } from './styles';

export const GroupLink = () => {
    return(
        <Container>
            <Text>group link</Text>
        </Container>
    );
}