import React from 'react';
import { Container, Input } from './styles';

export const TinyInput = (): any => {
    return(
        <Container>
            <Input>
            </Input>
        </Container>
    );
}