import React from 'react';
import { Container, Input } from './styles';

export const BigInput = (): any => {
    return(
        <Container>
            <Input>
            </Input>
        </Container>
    );
}