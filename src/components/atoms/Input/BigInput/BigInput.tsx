import React from 'react';
import { Container, Input } from './styles';

export const BigInput: React.FC = () => {
    return(
        <Container>
            <Input>
            </Input>
        </Container>
    );
}